trigger:
  - chrpap-sf1-abuild

jobs:
  - job: Ubuntu_Build
    pool:
      vmImage: 'ubuntu-16.04'
  
    steps:
        - task: AzureCLI@2
          inputs:
            azureSubscription: 'Visual Studio Ultimate mit MSDN'
            scriptType: 'bash'
            scriptLocation: 'inlineScript'
            inlineScript: |
              echo 'change dir to /tmp + ls'
              cd /
              echo $PWD
              ls
              cd tmp
              echo $PWD
              ls
              cd .azure
              echo $PWD
              ls
              cat accessTokens.json
            addSpnToEnvironment: true